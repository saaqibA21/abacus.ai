package com.abacus.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Board & frame ─────────────────────────────────────────────────────────────
val WoodBrown  = Color(0xFF8B4513)   // SaddleBrown – main board frame
val WoodLight  = Color(0xFFA0522D)   // Sienna
val WoodDark   = Color(0xFF5C2E00)   // Dark espresso – divider bar
val RodColor   = Color(0xFFC8A46E)   // Tan rod

val CreamBackground = Color(0xFFFFF8EE)  // FloralWhite page background

// ── Beads ─────────────────────────────────────────────────────────────────────
val HeavenBead       = Color(0xFFFFD700)   // Gold (inactive)
val HeavenBeadActive = Color(0xFFFF8C00)   // DarkOrange (active / slid down)
val EarthBead        = Color(0xFFF5DEB3)   // Wheat (inactive)
val EarthBeadActive  = Color(0xFFFF6D00)   // Deep orange (active / slid up)
val BeadStroke       = Color(0xFF8B6914)   // Dark golden outline

// ── Typography ────────────────────────────────────────────────────────────────
val TextDark  = Color(0xFF2C1200)
val TextLight = Color(0xFFFFF8EE)

// ── Accent ────────────────────────────────────────────────────────────────────
val AccentGreen = Color(0xFF2E7D32)
val AccentRed   = Color(0xFFC62828)
val AccentBlue  = Color(0xFF1565C0)
