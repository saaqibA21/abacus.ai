<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme (Material3 via Compose; XML theme is minimal). -->
    <style name="Theme.AbacusApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/wood_brown</item>
        <item name="android:navigationBarColor">@color/wood_brown</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
